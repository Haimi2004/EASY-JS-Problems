/** Create a function that accepts an array and returns the last item in the array. */

function get_last_item(arr) {
    return arr[arr.length - 1];
}
console.log(get_last_item(['name','age','sex','address']))
