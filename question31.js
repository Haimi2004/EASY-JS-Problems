/** Create a function that takes a word 
 * and returns the new word without including the first character. */


function remove_first_char(word) {
    return word.slice(1);
}
 console.log(remove_first_char('Eyes'));